# Generated by gen:openapi
# THIS FILE IS GENERATED — DO NOT EDIT
# Source: packages/domain/prim.yaml + packages/domain/src/api.ts
# Regenerate: pnpm gen:openapi

openapi: 3.1.0
info:
  title: domain.prim.sh API
  version: 1.0.0
  description: Register domains, manage DNS, auto-TLS. Full domain lifecycle via API.
servers:
  - url: https://domain.prim.sh
security:
  - x402: []
components:
  securitySchemes:
    x402:
      type: apiKey
      in: header
      name: Payment-Signature
      description: |
        EIP-3009 signed payment authorization. Server issues a 402 challenge; client signs and retries.
        See https://prim.sh/pay for the full x402 payment flow.
  schemas:
    Error:
      type: object
      required:
        - error
      properties:
        error:
          type: object
          required:
            - code
            - message
          properties:
            code:
              type: string
              enum:
                - not_found
                - forbidden
                - invalid_request
                - cloudflare_error
                - rate_limited
                - domain_taken
                - quote_expired
                - registrar_error
                - registration_failed
              description: Machine-readable error code.
            message:
              type: string
              description: Human-readable error message.
    ActivateDomainResponse:
      type: object
      properties:
        zone_id:
          type: string
          description: Cloudflare zone ID.
        status:
          type: string
          enum:
            - pending
            - active
            - moved
          description: Updated zone status.
        activation_requested:
          type: boolean
          const: true
          description: Always true — activation was requested from Cloudflare.
      required:
        - zone_id
        - status
        - activation_requested
    BatchCreateEntry:
      type: object
      properties:
        type:
          type: string
          enum:
            - A
            - AAAA
            - CNAME
            - MX
            - TXT
            - SRV
            - CAA
            - NS
          description: DNS record type.
        name:
          type: string
          description: DNS record name.
        content:
          type: string
          description: DNS record value.
        ttl:
          description: TTL in seconds. Default 1 (auto).
          type: number
        proxied:
          description: Enable Cloudflare proxying. Default false.
          type: boolean
        priority:
          description: Priority for MX and SRV records.
          type: number
      required:
        - type
        - name
        - content
    BatchDeleteEntry:
      type: object
      properties:
        id:
          type: string
          description: ID of the record to delete.
      required:
        - id
    BatchRecordsRequest:
      type: object
      properties:
        create:
          description: Records to create.
          type: array
          items:
            $ref: "#/components/schemas/BatchCreateEntry"
        update:
          description: Records to update.
          type: array
          items:
            $ref: "#/components/schemas/BatchUpdateEntry"
        delete:
          description: Records to delete.
          type: array
          items:
            $ref: "#/components/schemas/BatchDeleteEntry"
    BatchRecordsResponse:
      type: object
      properties:
        created:
          type: array
          items:
            $ref: "#/components/schemas/GetRecordResponse"
          description: Successfully created records.
        updated:
          type: array
          items:
            $ref: "#/components/schemas/GetRecordResponse"
          description: Successfully updated records.
        deleted:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
            required:
              - id
          description: IDs of deleted records.
      required:
        - created
        - updated
        - deleted
    BatchUpdateEntry:
      type: object
      properties:
        id:
          type: string
          description: ID of the record to update.
        content:
          description: New DNS record value.
          type: string
        ttl:
          description: New TTL in seconds.
          type: number
        proxied:
          description: Updated proxying flag.
          type: boolean
        priority:
          description: Updated priority.
          type: number
        type:
          description: Updated record type.
          type: string
          enum:
            - A
            - AAAA
            - CNAME
            - MX
            - TXT
            - SRV
            - CAA
            - NS
        name:
          description: Updated record name.
          type: string
      required:
        - id
    CreateRecordRequest:
      type: object
      properties:
        type:
          type: string
          enum:
            - A
            - AAAA
            - CNAME
            - MX
            - TXT
            - SRV
            - CAA
            - NS
          description: DNS record type.
        name:
          type: string
          description: DNS record name (hostname).
        content:
          type: string
          description: DNS record value.
        ttl:
          description: TTL in seconds. Default 1 (auto).
          type: number
        proxied:
          description: Enable Cloudflare proxying. Default false.
          type: boolean
        priority:
          description: Priority for MX and SRV records.
          type: number
      required:
        - type
        - name
        - content
    CreateZoneRequest:
      type: object
      properties:
        domain:
          type: string
          description: Domain name to create a zone for (e.g. "example.com").
      required:
        - domain
    CreateZoneResponse:
      type: object
      properties:
        zone:
          description: The created zone.
          $ref: "#/components/schemas/GetZoneResponse"
      required:
        - zone
    DkimKey:
      type: object
      properties:
        selector:
          type: string
          description: DKIM selector (e.g. "rsa2048").
        public_key:
          type: string
          description: DKIM public key string.
      required:
        - selector
        - public_key
    DomainSearchPrice:
      type: object
      properties:
        register:
          type: number
          description: Registration cost in USD.
        renew:
          description: Renewal cost in USD. Not returned by checkRegisterAvailability; omitted means unknown.
          type: number
        currency:
          type: string
          description: Currency code (e.g. "USD").
      required:
        - register
        - currency
    DomainSearchResult:
      type: object
      properties:
        domain:
          type: string
          description: Domain name queried.
        available:
          type: boolean
          description: Whether the domain is available for registration.
        price:
          description: Pricing info. Only present if available is true.
          $ref: "#/components/schemas/DomainSearchPrice"
        premium:
          description: Whether this is a premium domain with higher pricing.
          type: boolean
      required:
        - domain
        - available
    GetRecordResponse:
      type: object
      properties:
        id:
          type: string
          description: DNS record ID.
        zone_id:
          type: string
          description: Zone ID this record belongs to.
        type:
          type: string
          enum:
            - A
            - AAAA
            - CNAME
            - MX
            - TXT
            - SRV
            - CAA
            - NS
          description: DNS record type.
        name:
          type: string
          description: DNS record name (hostname, relative to zone).
        content:
          type: string
          description: DNS record value.
        ttl:
          type: number
          description: TTL in seconds.
        proxied:
          type: boolean
          description: Whether Cloudflare proxying is enabled.
        priority:
          anyOf:
            - type: number
            - type: "null"
          description: Priority for MX and SRV records. Null for other types.
        created_at:
          type: string
          description: ISO 8601 timestamp when the record was created.
        updated_at:
          type: string
          description: ISO 8601 timestamp when the record was last updated.
      required:
        - id
        - zone_id
        - type
        - name
        - content
        - ttl
        - proxied
        - priority
        - created_at
        - updated_at
    GetRegistrationStatusResponse:
      type: object
      properties:
        domain:
          type: string
          description: Domain name.
        purchased:
          type: boolean
          const: true
          description: Always true — only returned for registered domains.
        zone_id:
          anyOf:
            - type: string
            - type: "null"
          description: Cloudflare zone ID. Null if zone not yet created.
        zone_status:
          anyOf:
            - type: string
              enum:
                - pending
                - active
                - moved
            - type: "null"
          description: Current zone status. Null if zone not yet created.
        ns_configured_at_registrar:
          type: boolean
          description: Whether nameservers are configured at the registrar.
        ns_propagated:
          type: boolean
          description: Whether nameservers have propagated in DNS.
        ns_expected:
          type: array
          items:
            type: string
          description: Expected Cloudflare nameservers.
        ns_actual:
          type: array
          items:
            type: string
          description: Nameservers currently found in DNS.
        zone_active:
          type: boolean
          description: Whether the Cloudflare zone is active.
        all_ready:
          type: boolean
          description: Whether the domain is fully set up and ready.
        next_action:
          anyOf:
            - type: string
            - type: "null"
          description: Human-readable next action required. Null if all_ready is true.
      required:
        - domain
        - purchased
        - zone_id
        - zone_status
        - ns_configured_at_registrar
        - ns_propagated
        - ns_expected
        - ns_actual
        - zone_active
        - all_ready
        - next_action
    GetZoneResponse:
      type: object
      properties:
        id:
          type: string
          description: Cloudflare zone ID.
        domain:
          type: string
          description: Domain name (e.g. "example.com").
        status:
          type: string
          enum:
            - pending
            - active
            - moved
          description: 'Zone status: "pending" | "active" | "moved".'
        name_servers:
          type: array
          items:
            type: string
          description: Cloudflare nameservers to delegate to.
        owner_wallet:
          type: string
          description: Ethereum address of the zone owner.
        created_at:
          type: string
          description: ISO 8601 timestamp when the zone was created.
      required:
        - id
        - domain
        - status
        - name_servers
        - owner_wallet
        - created_at
    MailSetupRecordResult:
      type: object
      properties:
        type:
          type: string
          enum:
            - A
            - AAAA
            - CNAME
            - MX
            - TXT
            - SRV
            - CAA
            - NS
          description: DNS record type.
        name:
          type: string
          description: DNS record name.
        action:
          type: string
          enum:
            - created
            - updated
          description: Whether the record was created or updated.
      required:
        - type
        - name
        - action
    NsVerifyResult:
      type: object
      properties:
        expected:
          type: array
          items:
            type: string
          description: Expected Cloudflare nameservers.
        actual:
          type: array
          items:
            type: string
          description: Nameservers found in DNS.
        propagated:
          type: boolean
          description: Whether nameservers have propagated.
      required:
        - expected
        - actual
        - propagated
    QuoteRequest:
      type: object
      properties:
        domain:
          type: string
          description: Domain name to quote (e.g. "example.com").
        years:
          description: Number of years to register. Default 1.
          type: number
      required:
        - domain
    QuoteResponse:
      type: object
      properties:
        quote_id:
          type: string
          description: Quote ID to use when calling POST /v1/domains/register.
        domain:
          type: string
          description: Domain name quoted.
        available:
          type: boolean
          const: true
          description: Always true — quote is only returned for available domains.
        years:
          type: number
          description: Number of years in the quote.
        registrar_cost_usd:
          type: number
          description: Registrar cost in USD (internal cost).
        total_cost_usd:
          type: number
          description: Total cost in USD charged to the caller.
        currency:
          type: string
          description: Currency code (e.g. "USD").
        expires_at:
          type: string
          description: ISO 8601 timestamp when the quote expires. Use within the window to avoid quote_expired.
      required:
        - quote_id
        - domain
        - available
        - years
        - registrar_cost_usd
        - total_cost_usd
        - currency
        - expires_at
    RecordVerifyResult:
      type: object
      properties:
        type:
          type: string
          enum:
            - A
            - AAAA
            - CNAME
            - MX
            - TXT
            - SRV
            - CAA
            - NS
          description: DNS record type.
        name:
          type: string
          description: DNS record name.
        expected:
          type: string
          description: Expected DNS record value.
        actual:
          anyOf:
            - type: string
            - type: "null"
          description: Actual DNS record value found. Null if not found.
        propagated:
          type: boolean
          description: Whether the record has propagated.
      required:
        - type
        - name
        - expected
        - actual
        - propagated
    SearchDomainResponse:
      type: object
      properties:
        results:
          type: array
          items:
            $ref: "#/components/schemas/DomainSearchResult"
          description: Search results for each queried domain.
      required:
        - results
    SetupMailRequest:
      type: object
      properties:
        mail_server:
          type: string
          description: Mail server hostname (e.g. "mail.prim.sh").
        mail_server_ip:
          type: string
          description: Mail server IPv4 address (used for SPF record).
        dkim:
          description: DKIM keys to configure. Provide rsa and/or ed25519.
          type: object
          properties:
            rsa:
              description: RSA DKIM key.
              $ref: "#/components/schemas/DkimKey"
            ed25519:
              description: Ed25519 DKIM key.
              $ref: "#/components/schemas/DkimKey"
      required:
        - mail_server
        - mail_server_ip
    SetupMailResponse:
      type: object
      properties:
        records:
          type: array
          items:
            $ref: "#/components/schemas/MailSetupRecordResult"
          description: DNS records created or updated by the mail setup.
      required:
        - records
    UpdateRecordRequest:
      type: object
      properties:
        type:
          description: DNS record type.
          type: string
          enum:
            - A
            - AAAA
            - CNAME
            - MX
            - TXT
            - SRV
            - CAA
            - NS
        name:
          description: DNS record name.
          type: string
        content:
          description: DNS record value.
          type: string
        ttl:
          description: TTL in seconds.
          type: number
        proxied:
          description: Enable Cloudflare proxying.
          type: boolean
        priority:
          description: Priority for MX and SRV records.
          type: number
    VerifyDomainResponse:
      type: object
      properties:
        domain:
          type: string
          description: Domain name.
        nameservers:
          description: Nameserver propagation result.
          $ref: "#/components/schemas/NsVerifyResult"
        records:
          type: array
          items:
            $ref: "#/components/schemas/RecordVerifyResult"
          description: Per-record propagation results.
        all_propagated:
          type: boolean
          description: Whether all records and nameservers have propagated.
        zone_status:
          anyOf:
            - type: string
              enum:
                - pending
                - active
                - moved
            - type: "null"
          description: Current Cloudflare zone status. Null if zone not found.
      required:
        - domain
        - nameservers
        - records
        - all_propagated
        - zone_status
paths:
  /:
    get:
      operationId: healthCheck
      summary: Health check
      description: Returns service status. Free — no x402 payment required.
      security: []
      responses:
        "200":
          description: Service is running.
          content:
            application/json:
              schema:
                type: object
                required:
                  - service
                  - status
                properties:
                  service:
                    type: string
                    example: domain.sh
                  status:
                    type: string
                    example: ok
  /v1/domains/search:
    get:
      operationId: searchDomain
      summary: Check availability and pricing for a domain query
      security:
        - x402: []
      x-price: $0.001
      parameters:
        - name: query
          in: query
          description: Domain name or keyword to search
          schema:
            type: string
        - name: tlds
          in: query
          description: Comma-separated TLDs (e.g. com,xyz,io)
          schema:
            type: string
      responses:
        "200":
          description: Check availability and pricing for a domain query
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SearchDomainResponse"
        "402":
          description: x402 payment required.
        "502":
          description: NameSilo API error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /v1/domains/quote:
    post:
      operationId: quote
      summary: Get a 15-minute price quote for a domain
      security:
        - x402: []
      x-price: $0.001
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QuoteRequest"
      responses:
        "200":
          description: Get a 15-minute price quote for a domain
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuoteResponse"
        "400":
          description: Missing domain or invalid years
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "402":
          description: x402 payment required.
        "502":
          description: NameSilo API error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /v1/domains/{domain}/status:
    get:
      operationId: getDomainStatus
      summary: Full post-registration pipeline status (ns_propagated, zone_active, all_ready)
      security:
        - x402: []
      x-price: $0.001
      parameters:
        - name: domain
          in: path
          required: true
          description: domain parameter
          schema:
            type: string
      responses:
        "200":
          description: Full post-registration pipeline status (ns_propagated, zone_active, all_ready)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetRegistrationStatusResponse"
        "402":
          description: x402 payment required.
        "404":
          description: Domain not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /v1/zones:
    post:
      operationId: createZone
      summary: Create a Cloudflare DNS zone. Returns nameservers to set at your registrar.
      security:
        - x402: []
      x-price: $0.001
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateZoneRequest"
      responses:
        "201":
          description: Create a Cloudflare DNS zone. Returns nameservers to set at your registrar.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateZoneResponse"
        "400":
          description: Missing domain or invalid domain name
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "402":
          description: x402 payment required.
        "409":
          description: Zone already exists for this domain
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Cloudflare API error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    get:
      operationId: listZones
      summary: List DNS zones owned by the calling wallet (paginated)
      security:
        - x402: []
      x-price: $0.001
      parameters:
        - name: limit
          in: query
          description: 1-100, default 20
          schema:
            type: integer
        - name: after
          in: query
          description: Cursor from previous response
          schema:
            type: string
      responses:
        "200":
          description: List DNS zones owned by the calling wallet (paginated)
          content:
            application/json:
              schema:
                type: object
        "402":
          description: x402 payment required.
        "403":
          description: Resource belongs to a different wallet
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /v1/zones/{id}:
    get:
      operationId: getZone
      summary: Get zone details
      security:
        - x402: []
      x-price: $0.001
      parameters:
        - name: id
          in: path
          required: true
          description: id parameter
          schema:
            type: string
      responses:
        "200":
          description: Get zone details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetZoneResponse"
        "402":
          description: x402 payment required.
        "403":
          description: Resource belongs to a different wallet
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Zone not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    delete:
      operationId: deleteZone
      summary: Delete zone and all records. Irreversible.
      security:
        - x402: []
      x-price: $0.001
      parameters:
        - name: id
          in: path
          required: true
          description: id parameter
          schema:
            type: string
      responses:
        "200":
          description: Delete zone and all records. Irreversible.
          content:
            application/json:
              schema:
                type: object
        "402":
          description: x402 payment required.
        "403":
          description: Resource belongs to a different wallet
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Zone not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Cloudflare API error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /v1/zones/{zone_id}/activate:
    put:
      operationId: activateZone
      summary: Request Cloudflare NS re-check for faster activation
      security:
        - x402: []
      x-price: $0.001
      parameters:
        - name: zone_id
          in: path
          required: true
          description: zone_id parameter
          schema:
            type: string
      responses:
        "200":
          description: Request Cloudflare NS re-check for faster activation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ActivateDomainResponse"
        "402":
          description: x402 payment required.
        "403":
          description: Resource belongs to a different wallet
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Zone not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "429":
          description: Too many zone_activate calls
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /v1/zones/{zone_id}/verify:
    get:
      operationId: verifyZone
      summary: Check DNS propagation for all zone records
      security:
        - x402: []
      x-price: $0.001
      parameters:
        - name: zone_id
          in: path
          required: true
          description: zone_id parameter
          schema:
            type: string
      responses:
        "200":
          description: Check DNS propagation for all zone records
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VerifyDomainResponse"
        "402":
          description: x402 payment required.
        "403":
          description: Resource belongs to a different wallet
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Zone not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /v1/zones/{zone_id}/mail-setup:
    post:
      operationId: setupMail
      summary: Configure MX, SPF, DMARC, DKIM in one call. Idempotent.
      security:
        - x402: []
      x-price: $0.005
      parameters:
        - name: zone_id
          in: path
          required: true
          description: zone_id parameter
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetupMailRequest"
      responses:
        "200":
          description: Configure MX, SPF, DMARC, DKIM in one call. Idempotent.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SetupMailResponse"
        "400":
          description: Missing required mail server fields
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "402":
          description: x402 payment required.
        "403":
          description: Resource belongs to a different wallet
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Zone not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Cloudflare API error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /v1/zones/{zone_id}/records/batch:
    post:
      operationId: batchRecords
      summary: Create, update, and delete DNS records in one atomic request
      security:
        - x402: []
      x-price: $0.005
      parameters:
        - name: zone_id
          in: path
          required: true
          description: zone_id parameter
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BatchRecordsRequest"
      responses:
        "200":
          description: Create, update, and delete DNS records in one atomic request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BatchRecordsResponse"
        "400":
          description: Invalid record fields or values
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "402":
          description: x402 payment required.
        "403":
          description: Resource belongs to a different wallet
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Zone or record not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Cloudflare API error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /v1/zones/{zone_id}/records:
    post:
      operationId: createRecord
      summary: Create a DNS record (A, AAAA, CNAME, MX, TXT, SRV, CAA, NS)
      security:
        - x402: []
      x-price: $0.001
      parameters:
        - name: zone_id
          in: path
          required: true
          description: zone_id parameter
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateRecordRequest"
      responses:
        "201":
          description: Create a DNS record (A, AAAA, CNAME, MX, TXT, SRV, CAA, NS)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetRecordResponse"
        "400":
          description: Missing or invalid record fields
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "402":
          description: x402 payment required.
        "403":
          description: Resource belongs to a different wallet
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Zone not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Cloudflare API error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    get:
      operationId: listRecords
      summary: List all records in a DNS zone
      security:
        - x402: []
      x-price: $0.001
      parameters:
        - name: zone_id
          in: path
          required: true
          description: zone_id parameter
          schema:
            type: string
        - name: limit
          in: query
          description: 1-100, default 100
          schema:
            type: integer
        - name: after
          in: query
          description: Cursor from previous response
          schema:
            type: string
      responses:
        "200":
          description: List all records in a DNS zone
          content:
            application/json:
              schema:
                type: object
        "402":
          description: x402 payment required.
        "403":
          description: Resource belongs to a different wallet
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Zone not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /v1/zones/{zone_id}/records/{id}:
    get:
      operationId: getRecord
      summary: Get a single DNS record
      security:
        - x402: []
      x-price: $0.001
      parameters:
        - name: zone_id
          in: path
          required: true
          description: zone_id parameter
          schema:
            type: string
        - name: id
          in: path
          required: true
          description: id parameter
          schema:
            type: string
      responses:
        "200":
          description: Get a single DNS record
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetRecordResponse"
        "402":
          description: x402 payment required.
        "403":
          description: Resource belongs to a different wallet
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Zone or record not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: updateRecord
      summary: Update a DNS record
      security:
        - x402: []
      x-price: $0.001
      parameters:
        - name: zone_id
          in: path
          required: true
          description: zone_id parameter
          schema:
            type: string
        - name: id
          in: path
          required: true
          description: id parameter
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateRecordRequest"
      responses:
        "200":
          description: Update a DNS record
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetRecordResponse"
        "400":
          description: Invalid record fields
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "402":
          description: x402 payment required.
        "403":
          description: Resource belongs to a different wallet
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Zone or record not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Cloudflare API error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    delete:
      operationId: deleteRecord
      summary: Delete a DNS record
      security:
        - x402: []
      x-price: $0.001
      parameters:
        - name: zone_id
          in: path
          required: true
          description: zone_id parameter
          schema:
            type: string
        - name: id
          in: path
          required: true
          description: id parameter
          schema:
            type: string
      responses:
        "200":
          description: Delete a DNS record
          content:
            application/json:
              schema:
                type: object
        "402":
          description: x402 payment required.
        "403":
          description: Resource belongs to a different wallet
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Zone or record not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Cloudflare API error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
