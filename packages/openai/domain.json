[
  {
    "type": "function",
    "function": {
      "name": "domain_search",
      "description": "Check availability and pricing for a domain query. Price: $0.001 via x402 (USDC on Base).",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "Domain name or keyword to search"
          },
          "tlds": {
            "type": "string",
            "description": "Comma-separated TLDs (e.g. com,xyz,io)"
          }
        }
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "domain_quote",
      "description": "Get a 15-minute price quote for a domain. Price: $0.001 via x402 (USDC on Base).",
      "parameters": {
        "type": "object",
        "properties": {
          "domain": {
            "type": "string",
            "description": "Domain name to quote (e.g. \"example.com\")."
          },
          "years": {
            "type": "number",
            "description": "Number of years to register. Default 1."
          }
        },
        "required": [
          "domain"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "domain_status",
      "description": "Full post-registration pipeline status (ns_propagated, zone_active, all_ready). Price: $0.001 via x402 (USDC on Base).",
      "parameters": {
        "type": "object",
        "properties": {
          "domain": {
            "type": "string",
            "description": "domain parameter"
          }
        },
        "required": [
          "domain"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "domain_zone_create",
      "description": "Create a Cloudflare DNS zone. Returns nameservers to set at your registrar. Price: $0.05 via x402 (USDC on Base).",
      "parameters": {
        "type": "object",
        "properties": {
          "domain": {
            "type": "string",
            "description": "Domain name to create a zone for (e.g. \"example.com\")."
          }
        },
        "required": [
          "domain"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "domain_zone_list",
      "description": "List DNS zones owned by the calling wallet (paginated). Price: $0.001 via x402 (USDC on Base).",
      "parameters": {
        "type": "object",
        "properties": {
          "limit": {
            "type": "integer",
            "description": "1-100, default 20"
          },
          "after": {
            "type": "string",
            "description": "Cursor from previous response"
          }
        }
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "domain_zone_get",
      "description": "Get zone details. Price: $0.001 via x402 (USDC on Base).",
      "parameters": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "id parameter"
          }
        },
        "required": [
          "id"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "domain_zone_delete",
      "description": "Delete zone and all records. Irreversible. Price: $0.01 via x402 (USDC on Base).",
      "parameters": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "id parameter"
          }
        },
        "required": [
          "id"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "domain_zone_activate",
      "description": "Request Cloudflare NS re-check for faster activation. Price: $0.001 via x402 (USDC on Base).",
      "parameters": {
        "type": "object",
        "properties": {
          "zone_id": {
            "type": "string",
            "description": "zone_id parameter"
          }
        },
        "required": [
          "zone_id"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "domain_zone_verify",
      "description": "Check DNS propagation for all zone records. Price: $0.001 via x402 (USDC on Base).",
      "parameters": {
        "type": "object",
        "properties": {
          "zone_id": {
            "type": "string",
            "description": "zone_id parameter"
          }
        },
        "required": [
          "zone_id"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "domain_zone_mail_setup",
      "description": "Configure MX, SPF, DMARC, DKIM in one call. Idempotent. Price: $0.005 via x402 (USDC on Base).",
      "parameters": {
        "type": "object",
        "properties": {
          "zone_id": {
            "type": "string",
            "description": "zone_id parameter"
          },
          "mail_server": {
            "type": "string",
            "description": "Mail server hostname (e.g. \"mail.prim.sh\")."
          },
          "mail_server_ip": {
            "type": "string",
            "description": "Mail server IPv4 address (used for SPF record)."
          },
          "dkim": {
            "type": "object",
            "description": "DKIM keys to configure. Provide rsa and/or ed25519."
          }
        },
        "required": [
          "zone_id",
          "mail_server",
          "mail_server_ip"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "domain_record_batch",
      "description": "Create, update, and delete DNS records in one atomic request. Price: $0.005 via x402 (USDC on Base).",
      "parameters": {
        "type": "object",
        "properties": {
          "zone_id": {
            "type": "string",
            "description": "zone_id parameter"
          },
          "create": {
            "type": "array",
            "description": "Records to create.",
            "items": {
              "type": "object"
            }
          },
          "update": {
            "type": "array",
            "description": "Records to update.",
            "items": {
              "type": "object"
            }
          },
          "delete": {
            "type": "array",
            "description": "Records to delete.",
            "items": {
              "type": "object"
            }
          }
        },
        "required": [
          "zone_id"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "domain_record_create",
      "description": "Create a DNS record (A, AAAA, CNAME, MX, TXT, SRV, CAA, NS). Price: $0.001 via x402 (USDC on Base).",
      "parameters": {
        "type": "object",
        "properties": {
          "zone_id": {
            "type": "string",
            "description": "zone_id parameter"
          },
          "type": {
            "type": "string",
            "description": "DNS record type."
          },
          "name": {
            "type": "string",
            "description": "DNS record name (hostname)."
          },
          "content": {
            "type": "string",
            "description": "DNS record value."
          },
          "ttl": {
            "type": "number",
            "description": "TTL in seconds. Default 1 (auto)."
          },
          "proxied": {
            "type": "boolean",
            "description": "Enable Cloudflare proxying. Default false."
          },
          "priority": {
            "type": "number",
            "description": "Priority for MX and SRV records."
          }
        },
        "required": [
          "zone_id",
          "type",
          "name",
          "content"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "domain_record_list",
      "description": "List all records in a DNS zone. Price: $0.001 via x402 (USDC on Base).",
      "parameters": {
        "type": "object",
        "properties": {
          "zone_id": {
            "type": "string",
            "description": "zone_id parameter"
          }
        },
        "required": [
          "zone_id"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "domain_record_get",
      "description": "Get a single DNS record. Price: $0.001 via x402 (USDC on Base).",
      "parameters": {
        "type": "object",
        "properties": {
          "zone_id": {
            "type": "string",
            "description": "zone_id parameter"
          },
          "id": {
            "type": "string",
            "description": "id parameter"
          }
        },
        "required": [
          "zone_id",
          "id"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "domain_record_update",
      "description": "Update a DNS record. Price: $0.001 via x402 (USDC on Base).",
      "parameters": {
        "type": "object",
        "properties": {
          "zone_id": {
            "type": "string",
            "description": "zone_id parameter"
          },
          "id": {
            "type": "string",
            "description": "id parameter"
          },
          "type": {
            "type": "string",
            "description": "DNS record type."
          },
          "name": {
            "type": "string",
            "description": "DNS record name."
          },
          "content": {
            "type": "string",
            "description": "DNS record value."
          },
          "ttl": {
            "type": "number",
            "description": "TTL in seconds."
          },
          "proxied": {
            "type": "boolean",
            "description": "Enable Cloudflare proxying."
          },
          "priority": {
            "type": "number",
            "description": "Priority for MX and SRV records."
          }
        },
        "required": [
          "zone_id",
          "id"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "domain_record_delete",
      "description": "Delete a DNS record. Price: $0.001 via x402 (USDC on Base).",
      "parameters": {
        "type": "object",
        "properties": {
          "zone_id": {
            "type": "string",
            "description": "zone_id parameter"
          },
          "id": {
            "type": "string",
            "description": "id parameter"
          }
        },
        "required": [
          "zone_id",
          "id"
        ]
      }
    }
  }
]
