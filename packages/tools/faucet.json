[
  {
    "type": "function",
    "function": {
      "name": "faucet_usdc",
      "description": "Dispense 10 test USDC on Base Sepolia. Rate limit: once per 2 hours per address. Free.",
      "parameters": {
        "type": "object",
        "properties": {
          "address": {
            "type": "string",
            "description": "EVM wallet address to drip to (0x... 42 chars)."
          }
        },
        "required": [
          "address"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "faucet_eth",
      "description": "Dispense 0.01 test ETH on Base Sepolia. Rate limit: once per 1 hour per address. Free.",
      "parameters": {
        "type": "object",
        "properties": {
          "address": {
            "type": "string",
            "description": "EVM wallet address to drip to (0x... 42 chars)."
          }
        },
        "required": [
          "address"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "faucet_status",
      "description": "Check rate limit status for a wallet address across both faucets. Free.",
      "parameters": {
        "type": "object",
        "properties": {
          "address": {
            "type": "string",
            "description": "EVM wallet address (required)"
          }
        }
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "faucet_get_treasury_status",
      "description": "Check treasury wallet ETH balance and refill status. Free.",
      "parameters": {
        "type": "object",
        "properties": {}
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "faucet_refill_treasury",
      "description": "Batch-claim testnet ETH from Coinbase CDP faucet into treasury. Rate limited to once per 10 minutes. Free.",
      "parameters": {
        "type": "object",
        "properties": {}
      }
    }
  }
]
