[
  {
    "type": "function",
    "function": {
      "name": "mem_collection_create",
      "description": "Create a vector collection. Price: $0.001 via x402 (USDC on Base).",
      "parameters": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Collection name. Unique per wallet."
          },
          "distance": {
            "type": "string",
            "description": "Distance metric for similarity search. Default \"Cosine\".",
            "enum": [
              "Cosine",
              "Euclid",
              "Dot"
            ]
          },
          "dimension": {
            "type": "number",
            "description": "Vector dimension. Must match the embedding model used. Default 1536."
          }
        },
        "required": [
          "name"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "mem_collection_list",
      "description": "List collections owned by the calling wallet (paginated). document_count is null — use GET :id. Price: $0.001 via x402 (USDC on Base).",
      "parameters": {
        "type": "object",
        "properties": {
          "limit": {
            "type": "integer",
            "description": "1-100, default 20"
          },
          "after": {
            "type": "string",
            "description": "Cursor from previous response"
          }
        }
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "mem_collection_get",
      "description": "Get collection with live document_count from Qdrant. Price: $0.001 via x402 (USDC on Base).",
      "parameters": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "id parameter"
          }
        },
        "required": [
          "id"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "mem_collection_delete",
      "description": "Delete collection and all documents. Irreversible. Price: $0.001 via x402 (USDC on Base).",
      "parameters": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "id parameter"
          }
        },
        "required": [
          "id"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "mem_upsert",
      "description": "Embed and store documents. Each document: {id?, text, metadata?}. Existing IDs are replaced. Price: $0.0001 via x402 (USDC on Base).",
      "parameters": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "id parameter"
          },
          "documents": {
            "type": "array",
            "description": "Documents to upsert. Existing IDs are overwritten.",
            "items": {
              "type": "object"
            }
          }
        },
        "required": [
          "id",
          "documents"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "mem_query",
      "description": "Semantic search. Fields: text (required), top_k, filter (Qdrant native format). Price: $0.0001 via x402 (USDC on Base).",
      "parameters": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "id parameter"
          },
          "text": {
            "type": "string",
            "description": "Query text to embed and search against."
          },
          "top_k": {
            "type": "number",
            "description": "Number of nearest neighbors to return. Default 10."
          },
          "filter": {
            "type": "string",
            "description": "Qdrant-native filter passthrough."
          }
        },
        "required": [
          "id",
          "text"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "mem_cache_put",
      "description": "Store a value in the KV cache. Optional ttl in seconds for expiry. Price: $0.0001 via x402 (USDC on Base).",
      "parameters": {
        "type": "object",
        "properties": {
          "namespace": {
            "type": "string",
            "description": "namespace parameter"
          },
          "key": {
            "type": "string",
            "description": "key parameter"
          },
          "value": {
            "type": "string",
            "description": "Value to store. Any JSON-serializable value."
          },
          "ttl": {
            "type": "number",
            "description": "TTL in seconds. Omit or null for permanent."
          }
        },
        "required": [
          "namespace",
          "key",
          "value"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "mem_cache_get",
      "description": "Retrieve a cache value. Returns 404 if missing or expired. Price: $0.0001 via x402 (USDC on Base).",
      "parameters": {
        "type": "object",
        "properties": {
          "namespace": {
            "type": "string",
            "description": "namespace parameter"
          },
          "key": {
            "type": "string",
            "description": "key parameter"
          }
        },
        "required": [
          "namespace",
          "key"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "mem_cache_delete",
      "description": "Delete a cache entry. Price: $0.0001 via x402 (USDC on Base).",
      "parameters": {
        "type": "object",
        "properties": {
          "namespace": {
            "type": "string",
            "description": "namespace parameter"
          },
          "key": {
            "type": "string",
            "description": "key parameter"
          }
        },
        "required": [
          "namespace",
          "key"
        ]
      }
    }
  }
]
