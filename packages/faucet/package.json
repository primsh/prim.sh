{
  "name": "@primsh/faucet",
  "version": "0.0.0",
  "private": false,
  "type": "module",
  "main": "src/index.ts",
  "description": "Testnet USDC faucet for Base Sepolia",
  "repository": {
    "type": "git",
    "url": "https://github.com/primsh/prim",
    "directory": "packages/faucet"
  },
  "license": "Apache-2.0",
  "author": "Prim (https://prim.sh)",
  "keywords": ["prim", "x402", "faucet", "testnet", "usdc"],
  "scripts": {
    "dev": "bun run src/index.ts",
    "start": "bun run src/index.ts",
    "lint": "biome lint .",
    "format": "biome format .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest --run",
    "test:smoke": "vitest --run --passWithNoTests test/smoke-live.test.ts",
    "check": "pnpm lint && pnpm typecheck && pnpm test"
  },
  "dependencies": {
    "@coinbase/cdp-sdk": "^1.44.0",
    "@primsh/x402-middleware": "workspace:*",
    "hono": "^4.4.7",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "typescript": "^5.6.3",
    "vitest": "^1.6.1"
  }
}
