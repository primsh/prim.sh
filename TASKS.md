# TASKS

## Open

- [ ] Fix "Fourteen primitives" copy → should be "Nineteen primitives" (appears in manifesto x2 and CTA)
- [ ] Fix coral color class collision — `.c` is defined twice in CSS: once as `coral` and once as `#444` (comment gray), overriding coral-colored elements

## Done
