{
  "name": "prim",
  "version": "0.0.0",
  "private": true,
  "description": "Zero install. One curl. Infinite primitives. Infrastructure for autonomous agents — pay with USDC, use every service.",
  "homepage": "https://prim.sh",
  "author": "Primitive Shell (https://primitiveshell.com)",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/primsh/prim"
  },
  "keywords": [
    "prim",
    "x402",
    "agents",
    "infrastructure",
    "primitives",
    "ai"
  ],
  "scripts": {
    "lint": "biome lint .",
    "format": "biome format .",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "check": "pnpm -r lint && pnpm -r typecheck && pnpm -r test",
    "build:binaries": "pnpm --filter @primsh/cli build:binaries",
    "gen:openapi": "bun scripts/gen-openapi.ts",
    "gen:sdk": "bun scripts/gen-sdk.ts",
    "gen": "bun scripts/gen-openapi.ts && bun scripts/gen-prims.ts && bun scripts/gen-mcp.ts && bun scripts/gen-cli.ts && bun scripts/gen-tools.ts && bun scripts/gen-tests.ts && bun scripts/gen-docs.ts && bun scripts/gen-sdk.ts && bun scripts/gen-deploy.ts && bun scripts/gen-logos.ts && bun scripts/gen-og-images.ts",
    "gen:og": "bun scripts/gen-og-images.ts",
    "gen:prims": "bun scripts/gen-prims.ts",
    "gen:mcp": "bun scripts/gen-mcp.ts",
    "gen:cli": "bun scripts/gen-cli.ts",
    "gen:tools": "bun scripts/gen-tools.ts",
    "gen:tests": "bun scripts/gen-tests.ts",
    "gen:docs": "bun scripts/gen-docs.ts",
    "gen:logos": "bun scripts/gen-logos.ts",
    "gen:gate": "bun scripts/gen-gate.ts",
    "gen:deploy": "bun scripts/gen-deploy.ts",
    "gen:check": "bun scripts/gen-openapi.ts --check && bun scripts/gen-prims.ts --check && bun scripts/gen-mcp.ts --check && bun scripts/gen-cli.ts --check && bun scripts/gen-tools.ts --check && bun scripts/gen-tests.ts --check && bun scripts/gen-docs.ts --check && bun scripts/gen-sdk.ts --check && bun scripts/gen-deploy.ts --check",
    "push:github-profile": "bun scripts/gen-prims.ts && bun scripts/push-github-profile.ts",
    "gate": "bun scripts/gate-check.ts",
    "deploy:prim": "bun scripts/deploy-prim.ts",
    "check:openapi": "bun scripts/check-openapi.ts",
    "sync:vps": "bash scripts/sync-vps.sh",
    "sync:vps:dry": "bash scripts/sync-vps.sh --dry-run",
    "test:conformance": "bun scripts/conformance.ts",
    "create-prim": "bun scripts/create-prim.ts",
    "create-provider": "bun scripts/create-provider.ts",
    "prepare": "git config core.hooksPath .githooks 2>/dev/null || true",
    "clean": "rm -rf dist/ site-dist/ workers/platform/node_modules/",
    "clean:worktrees": "git worktree prune && git worktree list"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "@coinbase/cdp-sdk": "^1.44.1",
    "@types/node": "^25.3.0",
    "@vitest/coverage-v8": "^1.6.1",
    "ajv": "^8.18.0",
    "ajv-formats": "^3.0.1",
    "bun-types": "^1.3.10",
    "opentype.js": "^1.3.4",
    "typescript": "^5.6.3",
    "viem": "^2.21.0",
    "vitest": "^1.6.1"
  },
  "engines": {
    "bun": ">=1.0.0",
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@9.0.0",
  "dependencies": {
    "aws4fetch": "^1.0.20",
    "yaml": "^2.8.2"
  }
}
