{
  "name": "agentstack",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "lint": "biome lint .",
    "format": "biome format .",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "check": "pnpm -r lint && pnpm -r typecheck && pnpm -r test"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "typescript": "^5.6.3",
    "vitest": "^1.6.1"
  },
  "engines": {
    "bun": ">=1.0.0",
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}

