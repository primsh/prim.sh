# DigitalOcean Droplet IP (for firewall rules in R-2)
VPS_IP=

# Stalwart admin credentials
STALWART_ADMIN_USER=admin
STALWART_ADMIN_PASS=

# Stalwart API key for email.sh wrapper (created in R-2)
STALWART_API_USER=relay-wrapper
STALWART_API_KEY=

# Domain (configured in R-2)
MAIL_DOMAIN=email.prim.sh
# Mail server hostname (SMTP/IMAP/JMAP)
# MAIL_HOST=mail.prim.sh
