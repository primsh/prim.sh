# Testnet Integration Testing (W-10 non-custodial)
# Copy to .env.testnet, fill in values, then:
#   source .env.testnet && bun run scripts/integration-test.ts

# ─── Network (required: must be testnet) ─────────────────────────────────
PRIM_NETWORK=eip155:84532

# ─── Payment receiver address ────────────────────────────────────────────
# Any wallet you control on Base Sepolia. Receives x402 payments from the test agent.
PRIM_PAY_TO=0xYOUR_RECEIVER_WALLET_ADDRESS

# ─── Cloudflare / R2 (for store.sh — full test only) ─────────────────────
CLOUDFLARE_API_TOKEN=your_cf_api_token
CLOUDFLARE_ACCOUNT_ID=your_cf_account_id
R2_ACCESS_KEY_ID=your_r2_access_key
R2_SECRET_ACCESS_KEY=your_r2_secret_key

# ─── Agent key (auto-generated if missing) ───────────────────────────────
# Hex private key for the test agent. Generated on first run and appended here.
# Fund the derived address with test USDC via Circle faucet.
# AGENT_PRIVATE_KEY=0x...

# ─── Faucet (optional) ──────────────────────────────────────────────────
# ETH faucet requires a pre-funded treasury wallet (for gas drips).
# USDC faucet uses Circle's public API — no key needed.
# FAUCET_TREASURY_KEY=0xYOUR_FUNDED_SEPOLIA_PRIVATE_KEY

# ─── x402 Facilitator (optional) ─────────────────────────────────────────
# Default: Coinbase CDP. Override if settlement is flaky on testnet.
# FACILITATOR_URL=https://facilitator.payai.network

# ─── search.sh (SE-1) ────────────────────────────────────────────────────
# Required when running search.sh. Get from app.tavily.com.
# TAVILY_API_KEY=tvly-...

# ─── Optional overrides ──────────────────────────────────────────────────
# BASE_RPC_URL=https://sepolia.base.org     # defaults from PRIM_NETWORK
# WALLET_DB_PATH=./wallet-test.db           # defaults to ./wallet.db
# STORE_DB_PATH=./store-test.db             # defaults to ./store.db
# WALLET_PORT=3001
# FAUCET_PORT=3003
# STORE_PORT=3002
