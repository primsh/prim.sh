# Launch Status

Run the v0 launch readiness dashboard and report results.

## Instructions

Run `bun scripts/launch-status.ts` and output the results exactly as printed. Do not summarize or editorialize — just show the raw output.

Requires `VPS_IP` env var (set in `.env` or shell).
